package com.mobiquityinc.parser;

import com.mobiquityinc.model.PackageBean;

public class PackageParser {
    public static PackageBean parsePackage(String packageLine) {
        return new PackageBean(10);
    }
}
